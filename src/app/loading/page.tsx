import LoadingScreen from "@/components/loading/LoadingScreen";

export const metadata = {
  title: "Analyzing Your Market — Stratiq",
};

export default function LoadingPage() {
  return <LoadingScreen />;
}
